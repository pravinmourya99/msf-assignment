# Copy this file to backend.tf and fill in your bucket and key.
# Then run: terraform init -reconfigure
#
# backend "s3" {
#   bucket         = "your-terraform-state-bucket"
#   key            = "msf-assignment/dev/terraform.tfstate"
#   region         = "ap-south-1"
#   dynamodb_table = "terraform-state-lock"
#   encrypt        = true
# }
